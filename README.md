# MLB Player Evaluator

An MLB hitter analytics portal. The frontend lives in `web/`.

Quick start (Node 24):

    cd web
    npm ci
    npm run dev

More: [web/README.md](web/README.md) (frontend guide) and [_docs/](_docs/) (plan, process, performance notes).
